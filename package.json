{
  "name": "simpleid",
  "description": "A simple, personal OpenID provider written in PHP",
  "license": "GPL-2.0-or-later",
  "private": true,
  "scripts": {
    "build": "node build/build_assets.js",
    "watch": "node build/build_assets.js --watch",
    "serve": "node build/build_assets.js --watch --serve",
    "clean": "git restore www/html/assets"
  },
  "devDependencies": {
    "@compodoc/live-server": "^1.2.3",
    "arg": "^5.0.2",
    "esbuild": "^0.17.19"
  },
  "dependencies": {
    "alpinejs": "^3.12.1",
    "modern-normalize": "^2.0.0",
    "qrcode-svg": "^1.1.0"
  }
}
